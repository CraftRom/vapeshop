# Залежності наборів перевірки (qa/run_all.sh). У робочий образ не входять.
#
# Набори піднімають базу в SQLite через aiosqlite, а не в Postgres: так
# повний прогін не вимагає окремого сервера бази. Раніше цієї залежності не
# було записано ніде — на чистій машині всі Python-набори падали на імпорті
# ще до першої перевірки, і причину доводилось вгадувати.
-r requirements.txt
aiosqlite>=0.20
