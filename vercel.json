{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "//": "Один проєкт віддає і панель, і API з одного домену: ані CORS, ані ризику, що панель не знайде бекенд. Використано builds+routes, а не buildCommand+rewrites: у цій схемі 'dest' гарантовано доносить до функції початковий шлях запиту, тому FastAPI бачить /api/auth/login, а не переписаний шлях.",

  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": "backend/**",
        "maxLambdaSize": "50mb"
      }
    },
    {
      "src": "dashboard/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],

  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.py" },
    { "src": "/docs", "dest": "/api/index.py" },
    { "src": "/openapi.json", "dest": "/api/index.py" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],

  "crons": [
    { "path": "/api/cron/broadcast-tick", "schedule": "0 4 * * *" }
  ]
}
